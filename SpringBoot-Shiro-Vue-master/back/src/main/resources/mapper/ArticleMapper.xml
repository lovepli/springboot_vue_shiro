<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.heeexy.example.dao.ArticleDao">

    <insert id="addArticle" parameterType="com.alibaba.fastjson.JSONObject">
        INSERT INTO article
            (content)
        VALUES
            (#{content})
    </insert>



    <select id="countArticle" resultType="Integer">
        SELECT count(0)
        FROM article w
        WHERE w.delete_status = '1'
    </select>

    <select id="listArticle" resultType="com.alibaba.fastjson.JSONObject">
        SELECT
            w.id                                      id,
            w.content                                 content,
            date_format(w.create_time, '%Y.%m.%d %T') createTime
        FROM article w
        WHERE w.delete_status = '1'
        ORDER BY w.id DESC
        LIMIT #{offSet}, #{pageRow}
    </select>



    <update id="updateArticle" parameterType="com.alibaba.fastjson.JSONObject">
        UPDATE article
        SET
            content = #{content}
        WHERE id = #{id}
    </update>



    <!--查询文章列表 -->
    <select id="listArticle2"  parameterType="com.heeexy.example.util.model.query.ArticleQueryBean"   resultType="com.heeexy.example.util.model.vo.ArticleVO">
        SELECT
        w.*,
        w.id                                      id,
        w.content                                 content,
        date_format(w.create_time, '%Y.%m.%d %T') createTime,
        date_format(w.update_time, '%Y.%m.%d %T') updateTime,
        w.delete_status                           deleteStatus
        FROM article w
        WHERE w.delete_status = '1'
        ORDER BY w.id DESC
    </select>

    <insert id="addArticle2" parameterType="com.heeexy.example.util.model.Article" >
        INSERT INTO article
        (content)
        VALUES
        (#{content})
    </insert>

    <update id="updateArticle2" parameterType="com.heeexy.example.util.model.Article" >
        UPDATE article
        SET
        content = #{content}
        WHERE id = #{id}
    </update>

    <delete id="deleteArticle2" parameterType="com.heeexy.example.util.model.query.ArticleQueryBean">
        delete from article where id = #{id}
    </delete>
</mapper>